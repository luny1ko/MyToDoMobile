<Window x:Class="NotesApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Авторизация" Height="450" Width="400" 
        WindowStartupLocation="CenterScreen" Background="{DynamicResource MaterialDesignPaper}"
        WindowStyle="None" AllowsTransparency="True">

    <Border CornerRadius="20" Background="White" BorderThickness="1" BorderBrush="#E0E0E0">
        <Grid Margin="30">
            <StackPanel VerticalAlignment="Center">
                <materialDesign:PackIcon Kind="Lock" Width="60" Height="60" 
                                         HorizontalAlignment="Center" Foreground="{DynamicResource PrimaryHueMidBrush}"/>

                <TextBlock Text="Вход в систему" FontSize="24" FontWeight="Bold" 
                           HorizontalAlignment="Center" Margin="0,20"/>

                <TextBox x:Name="TxtUsername" materialDesign:HintAssist.Hint="Логин" 
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,10"/>

                <PasswordBox x:Name="TxtPassword" materialDesign:HintAssist.Hint="Пароль" 
                             Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" Margin="0,10"/>

                <Button x:Name="BtnLogin" Content="ВОЙТИ" Margin="0,30,0,10" Click="BtnLogin_Click"/>
                <Button Content="ВЫХОД" Style="{StaticResource MaterialDesignFlatButton}" Foreground="Gray" Click="BtnExit_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>