<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:MauiApp1.Models"
             x:Class="MauiApp1.MainPage"
             Title="MyToDo Задачи">

    <Grid RowDefinitions="Auto, *">
        <!-- Поиск -->
        <SearchBar x:Name="searchBar" Placeholder="Поиск..." TextChanged="SearchBar_TextChanged" Grid.Row="0"/>

        <!-- Список задач -->
        <CollectionView x:Name="tasksCollection" Grid.Row="1" SelectionMode="Single" SelectionChanged="OnSelectionChanged">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:TodoTask">
                    <Frame Margin="10,5" Padding="10" BorderColor="Transparent" BackgroundColor="White" HasShadow="True">
                        <Grid ColumnDefinitions="10, *, Auto">
                            <!-- Цвет приоритета -->
                            <BoxView Color="{Binding PriorityColor}" WidthRequest="5" Grid.Column="0" VerticalOptions="Fill"/>

                            <!-- Инфо -->
                            <VerticalStackLayout Grid.Column="1" Margin="10,0,0,0" Spacing="2">
                                <Label Text="{Binding Title}" FontSize="18" FontAttributes="Bold" TextColor="Black"/>
                                <Label Text="{Binding Project}" FontSize="12" TextColor="Gray"/>
                                <Label Text="{Binding Status}" FontSize="12" FontAttributes="Italic"/>
                            </VerticalStackLayout>

                            <!-- Дата -->
                            <Label Text="{Binding Date, StringFormat='{0:dd.MM}'}" Grid.Column="2" VerticalOptions="Center" FontSize="12"/>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Плавающая кнопка добавления -->
        <Button Text="+" 
                FontSize="30"
                WidthRequest="60" HeightRequest="60" CornerRadius="30"
                BackgroundColor="#f6c91f" TextColor="Black"
                Grid.Row="1" HorizontalOptions="End" VerticalOptions="End" Margin="20"
                Clicked="OnAddClicked"/>
    </Grid>
</ContentPage>